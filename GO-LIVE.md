# Go-Live — Boxing4Health Training Site

Everything is built, reviewed, and pushed to `main`. Here's what's done and the few things only you can finish.

## ☑️ What's live in the repo
- **Full site**: dashboard hub + 35 lessons across 5 modules + 1 resource, rebuilt from your Wix program and the "enhanced" GitHub content (enhanced content used where it existed).
- **Design system**: Boxing4Health blue branding, self-hosted fonts, Lucide icons, light/dark themes, and reader controls (text size, line spacing, high contrast, language) — all saved per device.
- **Bilingual (EN / fr-CA)**: interface, navigation, titles, summaries, and 15 lesson bodies fully translated; the 20 rich "enhanced" lessons show a French summary + an honest "translation under review" notice with the English body below.
- **Progression**: open-but-tracked — lessons complete on finishing a quiz or reaching the end; progress bars, "continue where you left off", and a printable **certificate** that unlocks at 100%.
- **Search, read-aloud, print styles, 404 page, noindex** (public but not search-indexed).
- **Self-contained**: no external calls except opt-in YouTube video embeds (click-to-load, privacy-friendly).

## ✅ Your morning checklist (~5 min)

### 1. Enable GitHub Pages
Repo → **Settings → Pages** → Source: **Deploy from a branch** → Branch: **main** / **/(root)** → Save. Turn on **Enforce HTTPS**.
The repo already contains a `CNAME` file (`training.boxing4health.com`), so Pages will pick up the custom domain automatically.

### 2. Add the DNS record (at your domain host — Wix, since boxing4health.com is a Wix site)
Add a **CNAME** record:
- **Host/Name:** `training`
- **Points to / Value:** `christine-cell.github.io`
- TTL: default
Leave all your existing `www` / root records untouched. Propagation can take 10 min–a few hours. Until then, the site is reachable at `https://christine-cell.github.io/b4h-web-content/`.

### 3. Review the French medical terms
Open `TRANSLATION-REVIEW.md`, confirm the flagged clinical terms, then clear the "translation under review" flag by setting `fr.reviewflag` to `""` in `assets/js/i18n.js` (both are already listed there).

## ⚠️ Two small content gaps to fill
- **Two "video" lessons** — *Boxing 101 (Video)* and *Assessment (Video)* — used Wix's native video player, which didn't expose a shareable URL during capture. Their text is intact; each shows a "video will be added here" note. To add a video, edit the lesson in `modules/` and drop in a block:
  ```html
  <div class="video" data-yt="YOUTUBE_ID" data-title="Boxing 101">
    <img class="video-poster" src="https://i.ytimg.com/vi/YOUTUBE_ID/hqdefault.jpg" alt="">
    <div class="video-play"><span data-icon="circle-play"></span></div>
  </div>
  ```
- **French for the 20 enhanced lessons** — the detailed bodies are still English (their built-in quizzes made a safe in-page French copy risky). This is the main remaining translation task; say the word and I'll complete it as a focused pass you can review.

## How to change things later
- Content/design rules live in `STYLE_GUIDE.md`; run edits through the `b4h-content` skill (`.claude/skills/b4h-content/`).
- Pages are generated by `tools/build_pages.py` (lessons) and `tools/build_hub.py` (hub) from `data/modules.json` + the French files in `i18n/`. Re-run them after edits, or just edit the built HTML directly for one-offs.
- Bump the `?v=` on CSS/JS links (or the `V` in the tools) when you change styles/scripts so browsers pick up changes.

## Original sources (archived, not served)
`_sources/` holds the full Wix export, both enhanced repos, and the reconciliation notes — nothing was lost.
